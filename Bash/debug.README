#! /bin/bash

# Use bash -x $file to debug the script
# Really Helpful tool
# Or, Use -x flag in the shebang for debugging natively

# You can also Use custom points from where to start debugging and where to stop debugging
# Use "set -x" to start debugging
# Use "set +x" to stop debugging
